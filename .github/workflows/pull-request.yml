name: Pull-requests

on:
  pull_request:
    branches: [ "main" ]

jobs:
  tests:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3

    - name: Run tests
      run: go test ./...

    - name: Build server
      run: go build cmd/httpserver/main.go
